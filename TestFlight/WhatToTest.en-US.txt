What to Test

- Poll rendering: single-choice and multiple-choice polls with vote bars, percentages, and voter count
- Poll voting: tap to vote (single), select + submit (multiple), remove vote
- Scroll stability: reduced page jumping when loading more posts
- Removed WebView snapshot fallback for unsupported content blocks
